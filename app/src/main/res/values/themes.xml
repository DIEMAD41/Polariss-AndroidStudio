<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Stores" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">#F0F0F0</item>
    </style>


    <style name="Theme.Stores" parent="Base.Theme.Stores" />

    <style name="Theme.APISplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logopolariss</item>
        <item name="windowSplashScreenAnimationDuration">3000</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Stores</item>
    </style>

    <style name="Theme.App" parent="Theme.Material3.DayNight">
        <item name="colorSurface">@color/polariss_red</item>
        <item name="colorOnSurfaceVariant">@color/white</item>
        <item name="colorOnSecondaryContainer">@color/polariss_red</item>
    </style>

    <style name="Theme.Inputs" parent="Base.Theme.Stores">
        <item name="colorPrimary">@color/polariss_red</item>
    </style>

</resources>